// Per-platform settings (this is the Claude build).
// The ChatGPT build replaces this file with platforms/chatgpt/config.js.
self.CF_CONFIG = {
  // ⚠️ Must match the extension ID registered at https://extensionpay.com
  extpayId: "mapph-claude-folders",
  // Page opened by the popup's "Open …" button and right after install.
  homeUrl: "https://claude.ai/recents",
  // Used for the backup file name.
  slug: "folders-for-claude"
};
